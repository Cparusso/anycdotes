<h2>All Users</h2>

<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.username, user %></li>
    Most Recent Location: <%= link_to user.locations.all.last.neighborhood, location_path(user.locations.all.last.id) %><br><br>
  <% end %>
</ul>


<%= link_to "New User", new_user_path %>
