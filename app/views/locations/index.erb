<h1 class="whitetext">All Locations</h1>

<div class="ui card commentcard">
  <ul>
    <% @locations.each do |location| %>
      <li><%= link_to location.neighborhood, location %> | (<%= location.stories.count %>)</li>
    <% end %>
  </ul>
</div>
