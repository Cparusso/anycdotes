<% if flash[:errors] %>
  <ul>
  <% flash[:errors].each do |e| %>
    <li><%= e %></li>
  <% end %>
  </ul>
<% end %>

<%= form_for @comment do |f| %>
  <%= f.text_area :content %>
  <%= f.hidden_field :story_id, value: flash[:story_id] %>
  <%= f.submit %>
<% end %>
